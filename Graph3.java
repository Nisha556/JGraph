package Graph;

public class Graph3 {
private Map<Vertex , List<Vertex>> adjVertices;
void addVertex(String label) {
	Vertex v = new Vertex(label);
	addvertices.values().stream().forEach(e -> e.remove(v));
	addVertices.remove(new Vertex(label));
	
	
}
}
